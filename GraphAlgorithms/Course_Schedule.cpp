#include <iostream>
#include <queue>
#include <vector>
using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;
    int x, y;
    vector<int> course(n);
    for(int i = 0; i < m; i++){
        cin >> x >> y;
    }
    vector<int> ans;
    return 0;
}