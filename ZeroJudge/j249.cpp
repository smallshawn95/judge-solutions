#include <iostream>
#include <queue>
#include <vector>
using namespace std;

int main()
{
    int m, n;
    cin >> m >> n;
    <vector<vector<int>> grid(n, m, 1);
    int p, q, a, b;
    pair<int, int> start;
    cin >> p >> q;
    for(int i = 0; i < p; i++){
        cin >> a >> b;
    }
    for(int i = 0; i < q; i++){
        cin >> a >> b;
    }
    cin >> start.first >> start.second;
    return 0;
}