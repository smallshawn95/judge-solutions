#include <iostream>
#include <vector>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int c, s, q, time = 1;
    while(cin >> c >> s >> q && c != 0){
        cout << "Case #" << time++ << '\n';
        for(int i = 0; i < s; i++){
            
        }
    }
    return 0;
}